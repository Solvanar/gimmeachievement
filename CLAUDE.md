Избегаем однобуквенных переменных - пишем осмысленные названия.
Отбиваем каждый return пустой строкой перед ним.
Отбиваем пустой строкой блок с if(...) {...}
Словари, массивы строковых или числовых констант, enum, Set - все статичные структуры
выносим в папку с константами constants, не выносим только специфиные для текущего файла/компонента константы/словари
